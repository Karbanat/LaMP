# LaMP
Для запуска приложения необходимо скачать файлы, в вируальной среде установить необходимые файлы командой _pip install -r requirements.txt_ затем использовать команду _cd backend_ и запустить сервер при помощи команды _uvicorn main:app --port 8000_
